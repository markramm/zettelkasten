[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cascade-research"
version = "0.1.0"
description = "Multi-KB research infrastructure for citizen journalists and AI agents"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Information Analysis",
]
keywords = ["knowledge-base", "zettelkasten", "research", "journalism", "ai"]

dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.1",
    "pydantic>=2.6.0",
    "python-dotenv>=1.0.1",
    "PyYAML>=6.0.1",
    "typer[all]>=0.12.3",
    "rich>=13.7.1",
    "watchdog>=4.0.1",
    "httpx>=0.27.0",
    "jinja2>=3.1.4",
]

[project.optional-dependencies]
ai = [
    "openai>=1.30.0",
    "anthropic>=0.30.0",
]
ui = [
    "streamlit>=1.34.0",
    "streamlit-agraph>=0.0.45",
]
semantic = [
    "sentence-transformers>=2.2.0",
    "sqlite-vec>=0.1.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "mypy>=1.9.0",
    "ruff>=0.3.0",
]
all = [
    "cascade-research[ai,ui,semantic,dev]",
]

[project.scripts]
cascade-research = "cascade_research.cli:main"
crk = "cascade_research.write_cli:main"
crk-read = "cascade_research.read_cli:main"
crk-server = "cascade_research.server.api:main"
crk-ui = "cascade_research.ui.run:main"

[project.urls]
Homepage = "https://github.com/transparency-cascade/cascade-research"
Documentation = "https://github.com/transparency-cascade/cascade-research#readme"
Repository = "https://github.com/transparency-cascade/cascade-research"

[tool.setuptools.packages.find]
where = ["."]
include = ["cascade_research*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4"]
ignore = [
    "E501",  # Line too long (handled by formatter)
    "B008",  # Function call in argument defaults (standard Typer pattern)
    "B904",  # raise-without-from in except (typer.Exit doesn't chain)
    "E741",  # Ambiguous variable name (l for link is clear in context)
    "N802",  # Function name should be lowercase (do_GET is HTTP convention)
]

[tool.ruff.lint.per-file-ignores]
# Test files can have unused assignments for clarity
# E402: importorskip must precede the conditional imports
"tests/*" = ["F841", "E402"]
